# Base URL of the Quipu Control Plane API. Leave empty (same-origin) when
# the UI is served by the same Cloud Run service as the API — see
# docs/architecture/control_plane_ui.md "Cloud Run deployment".
VITE_API_BASE_URL=http://localhost:8000
